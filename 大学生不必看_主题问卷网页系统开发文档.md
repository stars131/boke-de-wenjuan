# 《大学生不必看》主题问卷网页系统开发文档

> 文档用途：将《大学生不必看》视频播客的主题库转化为一套可部署到网页的问卷系统，用于收集目标观众对选题、经历、情绪状态、参与意愿和嘉宾线索的反馈，并为后续节目策划、嘉宾邀约、内容排期和数据复盘提供结构化数据。  
> 适用对象：AI 编程 Agent、全栈工程师、产品经理、设计师、运营人员。  
> 推荐技术栈：Next.js 14 App Router + React + TypeScript + Tailwind CSS + shadcn/ui + Prisma + PostgreSQL + Zod + React Hook Form + Recharts + Docker + Nginx。

---

## 1. 项目背景

《大学生不必看》是一档面向大四学生、研究生、刚毕业校友的视频播客，内容不是传统意义上的“大学生必看指南”，而是讨论大学生活中除了学习之外更真实、更隐秘、更容易被忽视的部分。

节目核心定位：

- 反说教：不做成功学、不做爹味建议。
- 强陪伴：让观众感觉“这件事不是只有我这样”。
- 弱结论：允许复杂、矛盾、犹豫和不确定。
- 强选题调研：用问卷和真实故事反向驱动节目选题。
- 可持续内容生产：通过问卷持续收集故事、痛点、嘉宾和观众需求。

本系统要解决的问题：

1. 观众对哪些主题最感兴趣？
2. 哪些主题最容易引发真实故事和讨论？
3. 哪些问题适合做首季正片？
4. 哪些观众愿意匿名投稿、接受采访或成为嘉宾？
5. 哪些选题更适合长视频、短切片、小红书图文或直播？
6. 如何将问卷结果沉淀为结构化选题库和内容决策依据？

---

## 2. 产品目标

### 2.1 用户侧目标

用户打开网页后，可以在 3 到 8 分钟内完成一份轻量但有表达空间的问卷。

用户应能完成以下行为：

- 选择自己最想看的节目主题。
- 给主题的重要性、共鸣度、讨论欲打分。
- 提交自己的真实经历或匿名故事。
- 表达是否愿意被节目组后续联系。
- 提交可选联系方式。
- 查看提交成功页，并获得“你可能最适合看的几期节目方向”的反馈。

### 2.2 创作者侧目标

节目组可以通过后台完成：

- 查看问卷总提交数。
- 查看不同主题的热度排名。
- 查看不同身份群体的主题偏好差异。
- 查看高价值长文本故事。
- 标记可采访线索。
- 导出 CSV / Excel。
- 生成节目选题优先级建议。
- 查看每个主题对应的短视频切片潜力。

### 2.3 技术目标

- 前后端完整可运行。
- 支持生产环境部署。
- 支持基础数据分析。
- 支持后续扩展为多问卷、多项目、多节目系统。
- 数据结构清晰，方便后续接入 AI 分析 Agent。
- 安全处理联系方式、隐私文本和敏感内容。

---

## 3. 目标用户

### 3.1 主要填写人群

| 用户类型 | 典型状态 | 核心需求 |
|---|---|---|
| 大四学生 | 毕业、找工作、考研、考公、离校焦虑 | 想看到同龄人真实表达 |
| 研究生 | 导学关系、论文、延期、就业压力 | 想确认自己的困境是否普遍 |
| 刚毕业 1-3 年校友 | 已经经历过转轨期 | 愿意回看和总结经验 |
| 普通大学生 | 大一到大三 | 想提前了解但不想被说教 |
| 潜在嘉宾 | 有故事、有表达欲 | 希望安全地讲述经历 |

### 3.2 管理后台用户

| 用户类型 | 权限 |
|---|---|
| 主理人 | 查看全部数据、导出、标记、分析 |
| 内容策划 | 查看主题热度、故事、嘉宾线索 |
| 剪辑/运营 | 查看高传播话题、短切片潜力 |
| 技术管理员 | 管理系统配置和数据备份 |

---

## 4. 问卷设计原则

### 4.1 问卷语气

整体语气必须符合《大学生不必看》的栏目气质：

- 不要像学校通知。
- 不要像心理测评。
- 不要像求职平台调研。
- 不要像品牌营销问卷。
- 要像一个同龄人认真问你：“这些事，你也经历过吗？”

示例语气：

不推荐：

> 请问您对以下大学生发展议题的关注程度如何？

推荐：

> 下面这些话题，如果我们真的找人坐下来聊一小时，你最想听哪几个？

### 4.2 问卷长度

推荐分为 6 个页面：

1. 开场说明
2. 基本身份
3. 主题兴趣选择
4. 深度打分
5. 故事投稿
6. 联系方式与提交成功

整体控制在：

- 快速填写：3 分钟
- 正常填写：5 分钟
- 深度投稿：8-12 分钟

### 4.3 隐私与安全

问卷必须明确：

- 可以匿名填写。
- 联系方式非必填。
- 故事可能被节目组选题参考，但不会直接公开个人身份。
- 如被引用，会做匿名化处理。
- 涉及心理危机、暴力、侵害等内容时，页面应提示寻求现实支持或专业帮助。

---

## 5. 问卷完整内容设计

## 5.1 开场页

### 标题

```text
《大学生不必看》选题问卷
```

### 副标题

```text
我们想聊一点大学里不总被认真讨论的事。
不是学习方法，不是成功经验，也不是“你应该怎样”。
而是那些你可能经历过、困惑过、但很少有人坐下来好好聊的东西。
```

### 说明文案

```text
这份问卷大约需要 3-8 分钟。

你可以匿名填写。
如果你愿意，也可以留下联系方式，之后我们可能邀请你参与节目、匿名访谈或故事征集。

你的回答会用于：
1. 决定《大学生不必看》第一季重点选题；
2. 寻找真实故事和嘉宾；
3. 判断哪些话题更适合长视频、短视频或图文内容。

不会用于：
1. 公开你的个人身份；
2. 未经同意直接展示你的联系方式；
3. 对你做任何评价或标签化判断。
```

### 按钮

```text
开始填写
```

---

## 5.2 基本身份页

### Q1. 你现在的身份是？

类型：单选  
必填：是

选项：

- 大一
- 大二
- 大三
- 大四 / 即将毕业
- 研一
- 研二
- 研三 / 即将毕业
- 已毕业 1 年内
- 已毕业 1-3 年
- 已毕业 3 年以上
- 其他

字段名：

```ts
identityStatus
```

---

### Q2. 你现在最接近哪种状态？

类型：多选  
必填：否  
最多可选：5

选项：

- 正在找工作
- 正在实习
- 准备考研 / 已考研
- 准备考公 / 考编
- 准备出国 / 留学
- 正在写论文 / 做课题
- 正在处理毕业相关事务
- 正在租房 / 搬家 / 换城市
- 正在经历感情问题
- 正在经历家庭压力
- 正在做副业 / 创业 / 自媒体
- 只是有点迷茫
- 暂时还挺稳定
- 其他

字段名：

```ts
currentStates
```

---

### Q3. 你更常在哪些平台看这类内容？

类型：多选  
必填：否

选项：

- B站
- 小红书
- 抖音
- 微信公众号
- 视频号
- 微博
- 小宇宙 / 播客平台
- 即刻
- 豆瓣
- 知乎
- YouTube
- 其他

字段名：

```ts
contentPlatforms
```

---

### Q4. 你更喜欢哪种内容形式？

类型：多选  
必填：否  
最多可选：4

选项：

- 45-65 分钟完整视频播客
- 10 分钟左右精华切片
- 1 分钟以内短视频
- 图文总结
- 真实故事投稿
- 主持人单人表达
- 多人圆桌讨论
- 专业嘉宾答疑
- 直播连麦
- 匿名来信回应

字段名：

```ts
preferredFormats
```

---

## 5.3 主题兴趣选择页

### 页面说明

```text
下面这些主题都可能成为《大学生不必看》的节目选题。
请选择你最想看的 5-8 个。
不需要选“正确”的，只选你真的会点进去看的。
```

### Q5. 你最想看哪些主题？

类型：多选卡片  
必填：是  
最少选择：3  
最多选择：8

#### 主题分组 A：心理与身份

1. 毕业身份卸载期  
   描述：从“学生”变成“要自己负责的大人”，最难受的到底是什么？

2. 求职失序与自我价值  
   描述：没有 offer 的时候，为什么那么容易怀疑自己？

3. 导学关系与论文焦虑  
   描述：研究生真正难熬的，可能不是忙，而是边界不清。

4. 延毕、gap 与失败叙事  
   描述：晚一点毕业、空窗一年，是不是就等于掉队？

5. 睡眠拖延、手机依赖与时间感崩坏  
   描述：熬夜到底是在玩手机，还是在偷回一点属于自己的时间？

#### 主题分组 B：关系与家庭

6. 社交焦虑与不会开口  
   描述：不是没有朋友，而是不知道如何开始关系。

7. 恋爱中的边界与危险信号  
   描述：很多控制，最开始都被包装成了“在乎”。

8. 分手、和解与情绪恢复  
   描述：一段关系结束后，怎样慢慢恢复日常？

9. 异地关系与人生分岔  
   描述：毕业后，感情最怕的是距离，还是不同步？

10. 家庭期待：考编、留城与婚育  
    描述：当家里说“稳定一点”，他们到底在说什么？

#### 主题分组 C：工作与钱

11. 暑期实习前移与履历焦虑  
    描述：没拿到实习，是不是已经晚了？

12. 实习权益、合同与试用期  
    描述：第一份工作前，哪些坑必须提前知道？

13. 兼职、副业与第一桶金  
    描述：大学里靠自己赚钱，会改变什么？

14. 生活费、消费、储蓄与网贷边界  
    描述：钱不够、想自由、又不想一直依赖家里。

15. 租房、搬家与落脚第一城  
    描述：毕业后第一件现实大事，可能是“住哪里”。

#### 主题分组 D：社群与网络

16. 社团、学生工作与校园文化  
    描述：学生工作到底带来了成长，还是消耗？

17. 爱好、手作、运动与不功利成长  
    描述：一个“不赚钱但开心”的爱好，还有没有必要？

18. 社交媒体分身与人设疲劳  
    描述：朋友圈、小红书、微博里的你，哪个才是真的你？

19. 容貌、身材焦虑与被看见的压力  
    描述：被看见、被比较、被评价，如何影响我们？

20. 人脉、内推与弱连接经营  
    描述：向别人求助，为什么总让人觉得羞耻？

#### 主题分组 E：生活方式与未来

21. 性教育、同意与安全  
    描述：大学里最该认真讨论、却总被含糊带过的话题。

22. 环保、公益与小型行动主义  
    描述：不是每个人都要改变世界，但可以从身边开始。

23. 创业、一人公司与试错成本  
    描述：大学生创业最该先想清楚什么？

24. 旅行、毕业仪式与告别 ritual  
    描述：为什么“最后一次”总让人那么上头？

25. 生活方式设计  
    描述：比起选哪份工作，你可能更想知道自己想过怎样的一天。

字段名：

```ts
selectedTopics
```

数据结构：

```ts
type TopicId =
  | "identity-unloading"
  | "job-search-self-worth"
  | "advisor-thesis-anxiety"
  | "delayed-graduation-gap"
  | "sleep-phone-time-collapse"
  | "social-anxiety"
  | "relationship-boundaries"
  | "breakup-recovery"
  | "long-distance-divergence"
  | "family-expectations"
  | "internship-anxiety"
  | "internship-rights"
  | "part-time-side-income"
  | "living-expense-consumption"
  | "renting-first-city"
  | "clubs-student-work"
  | "hobbies-non-utilitarian-growth"
  | "social-media-persona-fatigue"
  | "appearance-body-anxiety"
  | "networking-referrals"
  | "sex-education-consent-safety"
  | "public-good-small-actions"
  | "startup-one-person-company"
  | "graduation-travel-ritual"
  | "lifestyle-design";
```

---

## 5.4 主题深度打分页

该页面只展示用户在 Q5 中选过的主题。

### Q6. 对你选中的每个主题，请打三个分

类型：矩阵评分  
必填：是  
评分范围：1-5

每个主题有三个维度：

| 维度 | 说明 |
|---|---|
| 想看程度 | 如果出这一期，你会不会点开 |
| 共鸣程度 | 这个话题和你自己的经历有多近 |
| 讨论欲 | 你愿不愿意听别人深入聊，甚至自己也想聊 |

字段名：

```ts
topicRatings
```

数据结构：

```ts
type TopicRating = {
  topicId: TopicId;
  interestScore: number; // 1-5
  resonanceScore: number; // 1-5
  discussionScore: number; // 1-5
};
```

---

### Q7. 如果只能选 1 个你最希望第一季先做的主题，你会选哪个？

类型：单选  
必填：是  
选项：用户 Q5 中已选主题

字段名：

```ts
topPriorityTopic
```

---

### Q8. 哪个主题你觉得“不一定想公开说，但很想听别人聊”？

类型：单选 + 可选补充  
必填：否  
选项：全部 25 个主题 + “没有”

字段名：

```ts
privateButWantToHearTopic
```

补充字段：

```ts
privateButWantToHearReason
```

占位提示：

```text
可以简单说说为什么。比如：太私人、不知道怎么表达、怕被评价、身边没人聊……
```

---

## 5.5 故事投稿页

### 页面说明

```text
这一页可以不填。
但如果你愿意写一点真实经历，它会非常帮助我们判断：
这个主题到底应该怎么聊，应该找什么样的人来聊。
```

---

### Q9. 你有没有一个和这些主题有关的真实经历？

类型：长文本  
必填：否  
字数建议：50-1000 字

字段名：

```ts
personalStory
```

占位提示：

```text
可以写一件具体的小事：
某次搬寝室、某次面试失败、某段关系里的争吵、某次和父母通电话、某个熬夜到天亮的晚上……
不需要写得完整，也不需要写得漂亮。
```

---

### Q10. 这个经历更接近哪个主题？

类型：单选  
必填：当 Q9 有内容时必填  
选项：全部 25 个主题

字段名：

```ts
storyRelatedTopic
```

---

### Q11. 如果这个故事被节目参考，你希望如何处理？

类型：单选  
必填：当 Q9 有内容时必填

选项：

- 可以匿名引用大意
- 可以匿名改编为来信
- 只供节目组选题参考，不要公开
- 希望先联系我确认
- 不确定

字段名：

```ts
storyUsagePreference
```

---

### Q12. 这个故事的情绪强度大概是多少？

类型：滑杆 / 单选评分  
必填：否  
范围：1-5

说明：

- 1：只是普通经历
- 2：有一点触动
- 3：比较重要
- 4：当时很难受
- 5：现在想起来仍然影响很大

字段名：

```ts
storyEmotionIntensity
```

---

## 5.6 嘉宾与联系方式页

### Q13. 你愿意以哪种方式参与《大学生不必看》？

类型：多选  
必填：否

选项：

- 只填写问卷
- 匿名提供故事
- 接受文字访谈
- 接受语音访谈
- 接受视频连线
- 线下参与录制
- 推荐身边适合的人
- 暂时不确定

字段名：

```ts
participationWillingness
```

---

### Q14. 如果愿意被联系，请留下一个联系方式

类型：短文本  
必填：否

字段名：

```ts
contactInfo
```

占位提示：

```text
微信 / 邮箱 / 小红书号 / B站号 / 其他方式均可。非必填。
```

---

### Q15. 你希望我们如何称呼你？

类型：短文本  
必填：否

字段名：

```ts
nickname
```

占位提示：

```text
可以是假名、昵称、英文名，也可以不填。
```

---

### Q16. 还有什么你希望这个节目一定要聊的吗？

类型：长文本  
必填：否

字段名：

```ts
additionalSuggestions
```

占位提示：

```text
可以是一个主题、一句话、一个问题，或者你觉得“网上总是讲错了”的某件事。
```

---

## 5.7 提交成功页

### 标题

```text
收到。
这些“不必看”的事，我们会认真看。
```

### 内容

```text
谢谢你填完这份问卷。

你的回答会帮助我们决定《大学生不必看》第一季先聊什么，也会帮助我们找到真正值得坐下来慢慢聊的人和故事。

如果你留下了联系方式，我们可能会在后续选题中联系你。
如果你没有留下，也完全没关系，你的选择本身已经很有价值。
```

### 可选反馈模块

根据用户选择的 topPriorityTopic，展示对应推荐文案：

示例：

```text
你最想看的方向是：毕业身份卸载期。
这说明你可能更关心“从学生变成大人”的过渡感。
如果这一期上线，我们会重点聊：离校、失控、未来、身份变化，以及如何不急着给自己下结论。
```

### CTA

- 返回首页
- 分享问卷
- 关注节目更新
- 继续补充一个故事

---

## 6. 核心功能需求

## 6.1 前台问卷功能

### 6.1.1 多步骤问卷

必须支持：

- 步骤进度条
- 上一步 / 下一步
- 必填校验
- 多选数量限制
- 自动保存草稿到 localStorage
- 刷新页面后恢复填写进度
- 提交成功后清空草稿

步骤建议：

```ts
const FORM_STEPS = [
  "intro",
  "profile",
  "topic-selection",
  "topic-rating",
  "story",
  "contact",
  "success",
];
```

---

### 6.1.2 主题卡片

主题卡片字段：

```ts
type Topic = {
  id: TopicId;
  group: "心理与身份" | "关系与家庭" | "工作与钱" | "社群与网络" | "生活方式与未来";
  title: string;
  description: string;
  recommendedEpisode?: string;
  riskLevel: "low" | "medium" | "high";
};
```

卡片交互：

- 点击选中 / 取消。
- 选中后显示序号。
- 超过 8 个时提示。
- 少于 3 个时不能进入下一步。
- 支持按分组折叠。
- 移动端一列布局，桌面端三列布局。

---

### 6.1.3 评分组件

评分维度：

- 想看程度
- 共鸣程度
- 讨论欲

组件要求：

- 支持 1-5 分。
- 可以使用 RadioGroup、Slider 或星级按钮。
- 移动端易点击。
- 必须显示每个分值含义。
- 已选择主题数量较多时，应使用折叠卡片或分批显示，避免页面过长。

---

### 6.1.4 长文本输入

对 personalStory、additionalSuggestions 进行特殊处理：

- 字数统计。
- 最小建议字数提醒，不做强制。
- 最大 3000 字。
- 支持换行。
- 提交前进行前端长度校验。
- 后端再次校验。

---

### 6.1.5 提交防重复

要求：

- 提交按钮 loading 状态。
- 防止重复点击。
- 同一浏览器生成 anonymousSessionId。
- 后端保存 ipHash、userAgentHash，用于去重与风控。
- 不要直接保存明文 IP。

---

## 6.2 后台管理功能

后台路径：

```text
/admin
```

### 6.2.1 登录

初版可使用环境变量配置管理员账号密码。

```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=...
NEXTAUTH_SECRET=...
```

推荐使用：

- NextAuth / Auth.js
- Credentials Provider
- bcrypt 哈希密码
- HttpOnly Cookie
- CSRF 防护

---

### 6.2.2 数据概览 Dashboard

指标：

| 指标 | 说明 |
|---|---|
| 总提交数 | 所有有效问卷数量 |
| 今日提交数 | 当日新增 |
| 完整填写率 | 到达提交成功页的比例 |
| 平均选择主题数 | 反映用户兴趣广度 |
| 有故事提交数 | personalStory 非空 |
| 愿意被联系人数 | contactInfo 非空或 participationWillingness 包含访谈 |
| 高价值故事数 | 管理员标记为 highValue |
| 最热门主题 | 综合分最高的主题 |

---

### 6.2.3 主题热度分析

每个主题应计算：

```ts
topicHeatScore =
  selectedCount * 1
  + averageInterestScore * 20
  + averageResonanceScore * 15
  + averageDiscussionScore * 15
  + topPriorityCount * 5
  + storyCount * 3;
```

后台展示字段：

- 主题名称
- 所属分组
- 被选择次数
- 第一优先级次数
- 平均想看分
- 平均共鸣分
- 平均讨论欲分
- 相关故事数
- 综合热度分
- 推荐节目优先级

推荐节目优先级：

| 条件 | 优先级 |
|---|---|
| 高选择 + 高共鸣 + 高故事数 | S |
| 高选择 + 高想看 | A |
| 中等选择 + 高故事质量 | B |
| 低选择 + 高敏感 | C，谨慎处理 |
| 低选择 + 低评分 | 暂缓 |

---

### 6.2.4 故事管理

后台故事列表字段：

- 提交时间
- 昵称
- 身份状态
- 相关主题
- 情绪强度
- 故事摘要
- 使用偏好
- 是否愿意联系
- 管理员标签
- 处理状态

处理状态：

```ts
type StoryStatus =
  | "new"
  | "reviewed"
  | "potential-guest"
  | "selected-for-episode"
  | "do-not-use"
  | "archived";
```

管理员标签：

```ts
type StoryTag =
  | "高共鸣"
  | "强情绪"
  | "适合EP1"
  | "适合短视频"
  | "需要匿名"
  | "需要谨慎处理"
  | "可邀约嘉宾"
  | "法律/权益相关"
  | "心理支持相关";
```

功能：

- 搜索故事关键词。
- 按主题筛选。
- 按情绪强度筛选。
- 按参与意愿筛选。
- 按处理状态筛选。
- 管理员添加备注。
- 一键复制匿名摘要。
- 标记为可采访线索。

---

### 6.2.5 导出功能

支持导出：

1. 原始问卷 CSV。
2. 主题统计 CSV。
3. 故事投稿 CSV。
4. 嘉宾线索 CSV。
5. JSON 全量备份。

导出时必须：

- 默认隐藏 contactInfo。
- 只有超级管理员可导出联系方式。
- 导出文件名包含日期。

示例：

```text
university-podcast-survey-responses-2026-06-08.csv
```

---

### 6.2.6 AI 分析预留

后台应预留 AI 分析入口，但初版可以不接大模型。

预留接口：

```text
POST /api/admin/ai/topic-summary
POST /api/admin/ai/story-clustering
POST /api/admin/ai/episode-recommendations
```

未来 AI 可实现：

- 自动聚类故事。
- 自动生成每期选题建议。
- 自动提取高频关键词。
- 自动识别敏感内容。
- 自动生成嘉宾邀约话术。
- 自动生成节目大纲。

初版可以显示：

```text
AI 分析功能预留，当前版本暂未启用。
```

---

## 7. 页面结构

推荐路由：

```text
/
  问卷首页与填写流程

/success
  提交成功页，也可以作为问卷内部 step

/admin/login
  管理员登录

/admin
  数据概览

/admin/topics
  主题热度分析

/admin/responses
  问卷原始数据

/admin/stories
  故事投稿管理

/admin/exports
  数据导出

/admin/settings
  系统设置
```

---

## 8. UI 设计规范

### 8.1 视觉风格

关键词：

- 校园
- 真实
- 温和
- 克制
- 有一点反叛
- 不鸡血
- 不官方

建议色彩：

```ts
const theme = {
  background: "#F7F3EC",
  card: "#FFFFFF",
  textPrimary: "#1F2933",
  textSecondary: "#6B7280",
  accent: "#2F6B5F",
  accentLight: "#DDEBE7",
  warning: "#B45309",
  danger: "#B91C1C",
};
```

可以用 Tailwind 变量实现，不要硬编码在组件中。

### 8.2 字体

推荐：

- 中文：系统默认字体、思源黑体、苹方、HarmonyOS Sans
- 英文：Inter

CSS：

```css
font-family:
  Inter,
  ui-sans-serif,
  system-ui,
  -apple-system,
  BlinkMacSystemFont,
  "Segoe UI",
  "PingFang SC",
  "Hiragino Sans GB",
  "Microsoft YaHei",
  sans-serif;
```

### 8.3 组件风格

- 卡片圆角：16px-24px
- 页面最大宽度：960px
- 表单最大宽度：760px
- 按钮高度：44px-52px
- 移动端底部固定操作栏
- 桌面端卡片式布局

### 8.4 文案风格

按钮文案：

| 场景 | 文案 |
|---|---|
| 开始 | 开始填写 |
| 下一步 | 下一步 |
| 上一步 | 返回上一页 |
| 提交 | 提交问卷 |
| loading | 正在提交 |
| 成功 | 已收到 |
| 分享 | 分享给朋友 |
| 草稿恢复 | 已恢复上次未提交的内容 |

错误提示文案：

```text
至少选择 3 个主题。
最多选择 8 个主题。
这里还需要选一下。
字数有点长，可以稍微删掉一些。
提交失败了，可以稍后再试一次。
```

---

## 9. 技术架构

## 9.1 推荐技术栈

### 前端

- Next.js 14 App Router
- React 18+
- TypeScript
- Tailwind CSS
- shadcn/ui
- React Hook Form
- Zod
- Zustand，可选
- Recharts

### 后端

- Next.js Route Handlers
- Prisma
- PostgreSQL
- NextAuth / Auth.js
- bcrypt
- rate-limit
- zod server validation

### 部署

- Docker
- Docker Compose
- Nginx
- PostgreSQL volume
- 可选：systemd 管理服务

---

## 9.2 目录结构

```text
university-podcast-survey/
├── app/
│   ├── page.tsx
│   ├── layout.tsx
│   ├── globals.css
│   ├── success/
│   │   └── page.tsx
│   ├── admin/
│   │   ├── login/
│   │   │   └── page.tsx
│   │   ├── page.tsx
│   │   ├── topics/
│   │   │   └── page.tsx
│   │   ├── responses/
│   │   │   └── page.tsx
│   │   ├── stories/
│   │   │   └── page.tsx
│   │   ├── exports/
│   │   │   └── page.tsx
│   │   └── settings/
│   │       └── page.tsx
│   └── api/
│       ├── survey/
│       │   └── submit/
│       │       └── route.ts
│       ├── admin/
│       │   ├── stats/
│       │   │   └── route.ts
│       │   ├── topics/
│       │   │   └── route.ts
│       │   ├── responses/
│       │   │   └── route.ts
│       │   ├── stories/
│       │   │   └── route.ts
│       │   └── export/
│       │       └── route.ts
│       └── auth/
│           └── [...nextauth]/
│               └── route.ts
├── components/
│   ├── survey/
│   │   ├── SurveyShell.tsx
│   │   ├── IntroStep.tsx
│   │   ├── ProfileStep.tsx
│   │   ├── TopicSelectionStep.tsx
│   │   ├── TopicRatingStep.tsx
│   │   ├── StoryStep.tsx
│   │   ├── ContactStep.tsx
│   │   ├── SuccessStep.tsx
│   │   ├── TopicCard.tsx
│   │   ├── RatingMatrix.tsx
│   │   └── ProgressBar.tsx
│   ├── admin/
│   │   ├── AdminLayout.tsx
│   │   ├── StatsCards.tsx
│   │   ├── TopicHeatTable.tsx
│   │   ├── StoryTable.tsx
│   │   ├── ResponseTable.tsx
│   │   └── ExportPanel.tsx
│   └── ui/
├── lib/
│   ├── prisma.ts
│   ├── auth.ts
│   ├── validators/
│   │   └── survey.ts
│   ├── topics.ts
│   ├── analytics.ts
│   ├── export.ts
│   ├── privacy.ts
│   └── rate-limit.ts
├── prisma/
│   ├── schema.prisma
│   └── seed.ts
├── public/
├── middleware.ts
├── package.json
├── Dockerfile
├── docker-compose.yml
├── .env.example
└── README.md
```

---

## 10. 数据模型设计

## 10.1 Prisma Schema

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model SurveyResponse {
  id                         String   @id @default(cuid())
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt

  anonymousSessionId          String?
  identityStatus              String
  currentStates               String[]
  contentPlatforms            String[]
  preferredFormats            String[]

  selectedTopics              String[]
  topPriorityTopic            String
  privateButWantToHearTopic   String?
  privateButWantToHearReason  String?

  personalStory               String?
  storyRelatedTopic           String?
  storyUsagePreference        String?
  storyEmotionIntensity       Int?

  participationWillingness    String[]
  contactInfoEncrypted        String?
  contactInfoHash             String?
  nickname                    String?
  additionalSuggestions       String?

  ipHash                      String?
  userAgentHash               String?
  source                      String?
  referrer                    String?

  topicRatings                TopicRating[]
  adminNotes                  AdminNote[]
  storyReviews                StoryReview[]

  @@index([createdAt])
  @@index([topPriorityTopic])
  @@index([storyRelatedTopic])
  @@index([contactInfoHash])
}

model TopicRating {
  id                  String   @id @default(cuid())
  responseId           String
  response             SurveyResponse @relation(fields: [responseId], references: [id], onDelete: Cascade)

  topicId              String
  interestScore         Int
  resonanceScore        Int
  discussionScore       Int

  createdAt             DateTime @default(now())

  @@index([topicId])
  @@index([responseId])
}

model StoryReview {
  id                  String   @id @default(cuid())
  responseId           String
  response             SurveyResponse @relation(fields: [responseId], references: [id], onDelete: Cascade)

  status               String   @default("new")
  tags                 String[]
  isHighValue           Boolean  @default(false)
  isPotentialGuest      Boolean  @default(false)
  riskLevel             String   @default("normal")

  reviewedBy            String?
  reviewedAt            DateTime?
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  @@index([status])
  @@index([isHighValue])
  @@index([isPotentialGuest])
}

model AdminNote {
  id                  String   @id @default(cuid())
  responseId           String
  response             SurveyResponse @relation(fields: [responseId], references: [id], onDelete: Cascade)

  content              String
  createdBy            String?
  createdAt            DateTime @default(now())
}
```

---

## 10.2 联系方式加密

要求：

- contactInfo 不直接明文存储。
- 使用服务端密钥进行加密。
- 另存 contactInfoHash 用于去重。
- 导出时默认不包含明文联系方式。

环境变量：

```env
CONTACT_ENCRYPTION_KEY=base64-encoded-32-byte-key
```

工具函数：

```ts
encryptContactInfo(value: string): string
decryptContactInfo(value: string): string
hashContactInfo(value: string): string
```

---

## 11. API 设计

## 11.1 提交问卷

```http
POST /api/survey/submit
```

请求体：

```ts
type SubmitSurveyRequest = {
  anonymousSessionId?: string;
  identityStatus: string;
  currentStates: string[];
  contentPlatforms: string[];
  preferredFormats: string[];
  selectedTopics: string[];
  topicRatings: {
    topicId: string;
    interestScore: number;
    resonanceScore: number;
    discussionScore: number;
  }[];
  topPriorityTopic: string;
  privateButWantToHearTopic?: string;
  privateButWantToHearReason?: string;
  personalStory?: string;
  storyRelatedTopic?: string;
  storyUsagePreference?: string;
  storyEmotionIntensity?: number;
  participationWillingness: string[];
  contactInfo?: string;
  nickname?: string;
  additionalSuggestions?: string;
  source?: string;
  referrer?: string;
};
```

响应：

```ts
type SubmitSurveyResponse = {
  ok: true;
  responseId: string;
  recommendedTopic: {
    id: string;
    title: string;
    message: string;
  };
};
```

错误响应：

```ts
type ApiError = {
  ok: false;
  error: {
    code: string;
    message: string;
    fields?: Record<string, string>;
  };
};
```

---

## 11.2 管理后台统计

```http
GET /api/admin/stats
```

响应：

```ts
type AdminStatsResponse = {
  totalResponses: number;
  todayResponses: number;
  completionRate: number;
  averageSelectedTopics: number;
  storyCount: number;
  contactableCount: number;
  highValueStoryCount: number;
  topTopic: {
    topicId: string;
    title: string;
    heatScore: number;
  } | null;
};
```

---

## 11.3 主题统计

```http
GET /api/admin/topics
```

响应：

```ts
type TopicAnalyticsItem = {
  topicId: string;
  title: string;
  group: string;
  selectedCount: number;
  topPriorityCount: number;
  storyCount: number;
  averageInterestScore: number;
  averageResonanceScore: number;
  averageDiscussionScore: number;
  heatScore: number;
  priority: "S" | "A" | "B" | "C" | "Deferred";
};
```

---

## 11.4 故事列表

```http
GET /api/admin/stories?topicId=&status=&minEmotionIntensity=&q=&page=1&pageSize=20
```

响应：

```ts
type StoryListResponse = {
  items: {
    responseId: string;
    createdAt: string;
    nickname?: string;
    identityStatus: string;
    storyRelatedTopic?: string;
    personalStory: string;
    storyEmotionIntensity?: number;
    storyUsagePreference?: string;
    participationWillingness: string[];
    hasContactInfo: boolean;
    review?: {
      status: string;
      tags: string[];
      isHighValue: boolean;
      isPotentialGuest: boolean;
      riskLevel: string;
    };
  }[];
  pagination: {
    page: number;
    pageSize: number;
    total: number;
    totalPages: number;
  };
};
```

---

## 11.5 更新故事审核状态

```http
PATCH /api/admin/stories/:responseId/review
```

请求体：

```ts
type UpdateStoryReviewRequest = {
  status?: StoryStatus;
  tags?: string[];
  isHighValue?: boolean;
  isPotentialGuest?: boolean;
  riskLevel?: "normal" | "sensitive" | "high";
  note?: string;
};
```

---

## 11.6 导出

```http
GET /api/admin/export?type=responses|topics|stories|guest-leads&includeContact=false
```

要求：

- 必须登录。
- includeContact=true 需要超级管理员权限。
- 返回 CSV 文件。
- 设置正确文件名和 Content-Type。

---

## 12. 表单校验规则

使用 Zod。

```ts
import { z } from "zod";

export const submitSurveySchema = z.object({
  anonymousSessionId: z.string().max(128).optional(),
  identityStatus: z.string().min(1),
  currentStates: z.array(z.string()).max(5).default([]),
  contentPlatforms: z.array(z.string()).default([]),
  preferredFormats: z.array(z.string()).max(4).default([]),

  selectedTopics: z.array(z.string()).min(3).max(8),
  topicRatings: z.array(z.object({
    topicId: z.string(),
    interestScore: z.number().int().min(1).max(5),
    resonanceScore: z.number().int().min(1).max(5),
    discussionScore: z.number().int().min(1).max(5),
  })).min(3),

  topPriorityTopic: z.string().min(1),
  privateButWantToHearTopic: z.string().optional(),
  privateButWantToHearReason: z.string().max(1000).optional(),

  personalStory: z.string().max(3000).optional(),
  storyRelatedTopic: z.string().optional(),
  storyUsagePreference: z.string().optional(),
  storyEmotionIntensity: z.number().int().min(1).max(5).optional(),

  participationWillingness: z.array(z.string()).default([]),
  contactInfo: z.string().max(300).optional(),
  nickname: z.string().max(50).optional(),
  additionalSuggestions: z.string().max(2000).optional(),

  source: z.string().max(100).optional(),
  referrer: z.string().max(500).optional(),
}).superRefine((data, ctx) => {
  const selectedSet = new Set(data.selectedTopics);

  if (!selectedSet.has(data.topPriorityTopic)) {
    ctx.addIssue({
      code: z.ZodIssueCode.custom,
      path: ["topPriorityTopic"],
      message: "最想看的主题必须来自你已选择的主题。",
    });
  }

  for (const rating of data.topicRatings) {
    if (!selectedSet.has(rating.topicId)) {
      ctx.addIssue({
        code: z.ZodIssueCode.custom,
        path: ["topicRatings"],
        message: "评分主题必须来自你已选择的主题。",
      });
    }
  }

  if (data.personalStory && data.personalStory.trim().length > 0) {
    if (!data.storyRelatedTopic) {
      ctx.addIssue({
        code: z.ZodIssueCode.custom,
        path: ["storyRelatedTopic"],
        message: "写了故事后，请选择故事相关主题。",
      });
    }

    if (!data.storyUsagePreference) {
      ctx.addIssue({
        code: z.ZodIssueCode.custom,
        path: ["storyUsagePreference"],
        message: "写了故事后，请选择故事使用方式。",
      });
    }
  }
});
```

---

## 13. 数据分析逻辑

## 13.1 主题热度分

```ts
export function calculateTopicHeatScore(input: {
  selectedCount: number;
  averageInterestScore: number;
  averageResonanceScore: number;
  averageDiscussionScore: number;
  topPriorityCount: number;
  storyCount: number;
}) {
  return (
    input.selectedCount * 1 +
    input.averageInterestScore * 20 +
    input.averageResonanceScore * 15 +
    input.averageDiscussionScore * 15 +
    input.topPriorityCount * 5 +
    input.storyCount * 3
  );
}
```

---

## 13.2 主题优先级

```ts
export function getTopicPriority(input: {
  selectedCount: number;
  averageInterestScore: number;
  averageResonanceScore: number;
  averageDiscussionScore: number;
  storyCount: number;
  totalResponses: number;
}) {
  const selectedRate = input.totalResponses === 0
    ? 0
    : input.selectedCount / input.totalResponses;

  const avgScore =
    (input.averageInterestScore +
      input.averageResonanceScore +
      input.averageDiscussionScore) / 3;

  if (selectedRate >= 0.35 && avgScore >= 4.2 && input.storyCount >= 10) {
    return "S";
  }

  if (selectedRate >= 0.25 && avgScore >= 4.0) {
    return "A";
  }

  if (selectedRate >= 0.15 || input.storyCount >= 5) {
    return "B";
  }

  if (avgScore >= 4.0 && selectedRate < 0.15) {
    return "C";
  }

  return "Deferred";
}
```

---

## 13.3 嘉宾线索判断

```ts
export function isPotentialGuestLead(input: {
  participationWillingness: string[];
  contactInfo?: string | null;
  personalStory?: string | null;
  storyEmotionIntensity?: number | null;
}) {
  const willing =
    input.participationWillingness.includes("接受文字访谈") ||
    input.participationWillingness.includes("接受语音访谈") ||
    input.participationWillingness.includes("接受视频连线") ||
    input.participationWillingness.includes("线下参与录制");

  const hasContact = Boolean(input.contactInfo);
  const hasStory = Boolean(input.personalStory && input.personalStory.trim().length >= 80);
  const strongEmotion = (input.storyEmotionIntensity ?? 0) >= 3;

  return willing && hasContact && hasStory && strongEmotion;
}
```

---

## 14. 安全与隐私要求

### 14.1 必须实现

- 后端校验所有输入。
- 管理后台登录保护。
- 管理后台接口鉴权。
- 联系方式加密存储。
- IP 哈希存储，不保存明文 IP。
- 导出权限控制。
- 基础 rate limit。
- 防 XSS：前端展示长文本时不要 dangerouslySetInnerHTML。
- 防 SQL 注入：必须使用 Prisma。
- 生产环境强制 HTTPS。
- 禁止在日志中打印 contactInfo 和 personalStory 全文。

### 14.2 敏感内容处理

以下主题应标记为 high risk：

- 性教育、同意与安全
- 恋爱中的边界与危险信号
- 分手、创伤与情绪恢复
- 导学关系与论文焦虑
- 容貌、身材焦虑
- 网贷边界
- 家庭冲突
- 涉及自伤、暴力、侵害的投稿

系统不需要做专业心理干预，但应显示基础提示：

```text
如果你正在经历强烈痛苦、伤害风险或现实危险，请优先联系身边可信任的人、学校心理中心、当地紧急电话或专业机构。这个问卷不能替代现实帮助。
```

---

## 15. 环境变量

`.env.example`

```env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/university_podcast_survey"

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-random-secret"

ADMIN_USERNAME="admin"
ADMIN_PASSWORD_HASH="replace-with-bcrypt-hash"

CONTACT_ENCRYPTION_KEY="replace-with-base64-32-byte-key"
HASH_SECRET="replace-with-hash-secret"

APP_BASE_URL="http://localhost:3000"
NODE_ENV="development"
```

---

## 16. 部署方案

## 16.1 Dockerfile

```dockerfile
FROM node:20-alpine AS base

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

RUN npx prisma generate
RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]
```

---

## 16.2 docker-compose.yml

```yaml
version: "3.9"

services:
  db:
    image: postgres:16
    container_name: university_podcast_survey_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: university_podcast_survey
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  app:
    build: .
    container_name: university_podcast_survey_app
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - db
    ports:
      - "3000:3000"

volumes:
  postgres_data:
```

---

## 16.3 初始化命令

```bash
npm install
npx prisma generate
npx prisma migrate dev --name init
npm run dev
```

生产环境：

```bash
docker compose up -d --build
docker compose exec app npx prisma migrate deploy
```

---

## 17. Nginx 配置示例

```nginx
server {
    listen 80;
    server_name survey.example.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

建议生产环境使用 HTTPS：

```bash
certbot --nginx -d survey.example.com
```

---

## 18. 测试要求

### 18.1 单元测试

必须测试：

- Zod 表单校验。
- 热度分计算。
- 优先级计算。
- 联系方式加密与解密。
- 联系方式哈希稳定性。
- 故事线索判断。

### 18.2 集成测试

必须测试：

- 正常提交问卷。
- 少于 3 个主题无法提交。
- 超过 8 个主题无法提交。
- 写故事但不选故事主题无法提交。
- 后台未登录无法访问。
- 管理员可查看统计。
- 导出不包含联系方式。
- 超级管理员导出可包含联系方式。

### 18.3 E2E 测试

推荐使用 Playwright。

核心流程：

1. 用户进入首页。
2. 填写身份信息。
3. 选择 5 个主题。
4. 完成评分。
5. 填写故事。
6. 提交成功。
7. 管理员登录后台。
8. 后台看到新增数据。
9. 后台导出 CSV。

---

## 19. 性能要求

- 首页首屏加载时间小于 2 秒。
- 问卷交互无明显卡顿。
- 10000 条问卷数据内后台统计响应小于 1 秒。
- 导出大文件时支持流式响应或分页导出。
- topic analytics 应尽量通过 SQL 聚合完成，不要一次性拉取全部数据到内存。

---

## 20. 可访问性要求

- 所有输入框有 label。
- 所有按钮可键盘操作。
- 色彩对比度达标。
- 错误提示能被屏幕阅读器读到。
- 移动端点击区域不小于 44px。
- 评分组件不能只依赖颜色表达状态。

---

## 21. 运营配置

`lib/topics.ts` 应维护全部主题配置。

```ts
export const TOPICS = [
  {
    id: "identity-unloading",
    group: "心理与身份",
    title: "毕业身份卸载期",
    description: "从“学生”变成“要自己负责的大人”，最难受的到底是什么？",
    riskLevel: "medium",
  },
  {
    id: "job-search-self-worth",
    group: "心理与身份",
    title: "求职失序与自我价值",
    description: "没有 offer 的时候，为什么那么容易怀疑自己？",
    riskLevel: "medium",
  },
  {
    id: "advisor-thesis-anxiety",
    group: "心理与身份",
    title: "导学关系与论文焦虑",
    description: "研究生真正难熬的，可能不是忙，而是边界不清。",
    riskLevel: "high",
  }
];
```

完整实现时必须补全 25 个主题。

后台设置页应允许未来配置：

- 问卷是否开放。
- 是否允许故事投稿。
- 是否显示联系方式输入。
- 主题选择最小数量。
- 主题选择最大数量。
- 成功页文案。
- 分享标题。
- 分享描述。

初版可以写死在配置文件中。

---

## 22. SEO 与分享

页面 meta：

```ts
export const metadata = {
  title: "《大学生不必看》选题问卷",
  description: "我们想聊一点大学里不总被认真讨论的事。匿名填写，帮助决定第一季选题。",
  openGraph: {
    title: "《大学生不必看》选题问卷",
    description: "不是学习方法，不是成功经验，而是那些你可能经历过、困惑过、但很少有人认真聊的东西。",
    type: "website",
  },
};
```

分享卡片建议：

- 标题：大学生不必看，但这些事我们想认真聊聊
- 描述：一份关于毕业、关系、工作、钱、家庭和未来的匿名选题问卷
- 图片：校园黄昏、空教室、宿舍行李、对谈桌面风格

---

## 23. 开发里程碑

### Milestone 1：基础问卷 MVP

交付：

- 前台完整问卷流程
- 本地草稿
- 提交 API
- PostgreSQL 存储
- 成功页

验收：

- 用户可完整提交一次问卷。
- 数据库可看到完整记录。
- 必填和多选限制正常工作。

---

### Milestone 2：管理后台 MVP

交付：

- 登录
- 数据概览
- 主题热度表
- 原始问卷列表
- 故事列表

验收：

- 管理员可登录查看数据。
- 可以按主题查看热度排序。
- 可以查看故事投稿。

---

### Milestone 3：导出与审核

交付：

- CSV 导出
- 故事审核状态
- 管理员备注
- 嘉宾线索标记
- 联系方式加密

验收：

- 可导出主题统计和故事线索。
- 联系方式默认不明文导出。
- 管理员可标记高价值故事。

---

### Milestone 4：生产部署

交付：

- Dockerfile
- docker-compose
- Nginx 配置
- HTTPS
- 数据库迁移
- README 部署说明

验收：

- 可在服务器通过域名访问。
- HTTPS 正常。
- 重启后数据不丢失。
- 后台可正常访问。

---

## 24. AI 开发执行提示词

下面这段可以直接复制给 AI 编程工具使用。

```text
你是一名资深全栈工程师，请根据本 Markdown 文档开发一个企业级的《大学生不必看》主题问卷网页系统。

技术栈要求：
- Next.js 14 App Router
- React
- TypeScript
- Tailwind CSS
- shadcn/ui
- React Hook Form
- Zod
- Prisma
- PostgreSQL
- NextAuth / Auth.js
- Docker
- Nginx 反向代理部署支持

核心目标：
1. 实现一个多步骤问卷前台页面。
2. 问卷包含身份信息、主题选择、主题评分、故事投稿、联系方式和成功页。
3. 支持 25 个节目主题，按 5 个分组展示。
4. 主题选择限制为最少 3 个、最多 8 个。
5. 用户只需要给自己选择过的主题打分。
6. 支持 localStorage 草稿保存和恢复。
7. 后端使用 Zod 做二次校验。
8. 数据使用 Prisma 存入 PostgreSQL。
9. 联系方式必须加密存储，不能明文直接保存。
10. IP 只保存哈希，不保存明文。
11. 实现管理员登录。
12. 实现后台 Dashboard、主题热度分析、问卷列表、故事列表、导出功能。
13. 故事列表支持审核状态、标签、高价值故事、可采访线索标记。
14. 实现基础 rate limit 和接口鉴权。
15. 提供 Dockerfile、docker-compose.yml、.env.example 和 README。
16. 代码必须模块化、类型安全、可维护。
17. 生成完整可运行项目，不要只写示例片段。
18. 完成后请给出本地运行命令、数据库迁移命令、部署命令和主要文件说明。

请严格按照文档中的数据结构、字段名、问卷内容、API 设计和验收标准实现。
如果遇到文档未明确的细节，请采用生产级默认方案，不要中断开发。
```

---

## 25. 验收清单

### 前台

- [ ] 首页文案正确。
- [ ] 多步骤问卷可正常切换。
- [ ] 身份信息可填写。
- [ ] 25 个主题按 5 组展示。
- [ ] 主题选择少于 3 个不能进入下一步。
- [ ] 主题选择超过 8 个会提示。
- [ ] 评分页只显示已选主题。
- [ ] 写故事后必须选择故事相关主题和使用偏好。
- [ ] 联系方式非必填。
- [ ] 提交成功后显示反馈。
- [ ] 刷新后可恢复草稿。
- [ ] 提交后清空草稿。

### 后端

- [ ] 提交接口可用。
- [ ] Zod 校验生效。
- [ ] Prisma 数据写入正常。
- [ ] 联系方式加密。
- [ ] IP 哈希。
- [ ] 重复提交防护。
- [ ] Rate limit 生效。

### 后台

- [ ] 管理员可登录。
- [ ] 未登录不可访问后台接口。
- [ ] Dashboard 数据准确。
- [ ] 主题热度排序准确。
- [ ] 故事列表可筛选。
- [ ] 可标记故事状态和标签。
- [ ] 可导出 CSV。
- [ ] 默认导出不包含联系方式。
- [ ] 超级管理员可导出联系方式。

### 部署

- [ ] Docker 构建成功。
- [ ] docker compose 启动成功。
- [ ] PostgreSQL 数据持久化。
- [ ] Nginx 代理正常。
- [ ] HTTPS 正常。
- [ ] README 清晰。

---

## 26. 后续扩展方向

第一版完成后，可以继续扩展：

1. 多问卷系统：不同节目、不同季度、不同主题池。
2. 观众画像分析：身份、平台、主题之间的交叉分析。
3. AI 选题推荐：根据问卷数据自动生成首季排期。
4. AI 故事聚类：将投稿按情绪、主题和叙事结构聚类。
5. 嘉宾 CRM：管理邀约、沟通、录制状态。
6. 节目反馈问卷：每期发布后收集观众反馈。
7. 内容日历：把高热主题直接转为选题排期。
8. 短视频切片建议：根据主题评分和故事文本生成切片方向。
9. 公开数据页：展示匿名的主题热度榜，增强参与感。
10. 校园渠道追踪：为不同学校、社团、朋友圈链接生成 source 参数。

---

## 27. 结论

这套系统的本质不是一个普通问卷，而是《大学生不必看》的内容发动机。

它应该同时完成三件事：

1. 帮你判断观众真正想看什么。
2. 帮你找到值得深聊的人和故事。
3. 帮你把节目从“我想聊什么”升级为“我们这一代人正在共同经历什么”。

首版不要追求复杂功能，必须优先保证：

- 填写体验顺滑。
- 问题设计准确。
- 数据结构干净。
- 后台能帮助选题决策。
- 隐私处理可靠。

只要这五点成立，这个问卷就不仅能服务第一季，也能持续成为后续每一季的选题入口。
